name: barcode_validator
channels:
  - defaults
  - bioconda
  - conda-forge
dependencies:
  - biopython
  - requests
  - gspread
  - oauth2client
  - pyyaml
  - pandas
  - hmmer
  - xlrd=1.1.0
  - pip
  - pip:
    - subprocess.run
    - boldigger2
