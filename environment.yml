name: barcode_validator
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - bioconda::blast
  - bioconda::hmmer
  - conda-forge::pip
  - pip:
      - -r requirements.txt
